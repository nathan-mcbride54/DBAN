[workspace]
resolver = "2"
members = ["crates/scour-core", "crates/scour"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://example.invalid/scour"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
